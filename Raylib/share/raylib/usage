The package raylib:x86-windows provides CMake targets:

    find_package(raylib CONFIG REQUIRED)
    target_include_directories(main PRIVATE ${raylib_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${raylib_LIBRARIES})
